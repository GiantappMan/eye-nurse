export class Setting {
  //休息间隔
  RestInterval?: string;
  //休息时长
  RestDuration?: string;
  //开机启动
  RunWhenStarts?: boolean;
  //自动重置
  ResetWhenSessionUnlock?: boolean;
  //重置超时时间
  ResetTimeout?: string;
}
